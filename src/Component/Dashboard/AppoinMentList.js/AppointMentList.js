import React, { useState } from 'react';

const AppointMentList = ({appointMent}) => {
    
    return (
        <div>
            {
                appointMent.length
            }
        </div>
    );
};

export default AppointMentList;